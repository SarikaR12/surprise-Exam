#include<stdio.h>
main()
	{
		int i,n,temp,ans;
		printf("enter value n = ");
		scanf("%d",&i);
		temp=n;
		for(i=n>1)
		if(i<=0)
		{
			printf("0 is nagative");
		}
		else
		{
			printf("1 is posetive element");
		}
	}